# randomColor

生成一个随机的 6 位十六进制颜色值。

## 基础用法

```ts
randomColor() // '#f8e2e8'
randomColor() // '#1a2b3c'
```

## API

返回值|<code>string</code>
:- | :-

## 注意事项

- 基于 `Math.random`，**不是加密安全的随机源**；做随机主题色这类装饰性用途没问题，但不要用它生成需要不可预测的颜色标识。
- 返回值恒为 `#` 开头的 6 位十六进制（不足 6 位时右侧补 `0`），可以直接交给 CSS 解析。
