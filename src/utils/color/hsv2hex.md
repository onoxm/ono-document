# hsv2hex

把 `hsv` 颜色转换为十六进制，内部先转 `rgb` 再转 `hex`。

## 基础用法

```ts
hsv2hex('hsv(240, 100, 100)') // '#0000ff'
```

## API

参数|说明|类型|默认值|是否必填
:- | :- | :- | :- | :-
hsv|`hsv(...)` 颜色字符串|<code>string</code>|-|是

## 注意事项

- 等价于 `rgb2hex(hsv2rgb(hsv))`，返回 6 位不带透明度的十六进制。
