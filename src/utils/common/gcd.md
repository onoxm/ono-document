# gcd
计算最大公约数

## 示例
```ts
const a = 10
const b = 20
const c = 5

gcd(a, b) // 10
gcd(b, c) // 5
```

## 参数
参数|说明|类型|默认值|是否必填
:- | :- | :- | :- | :-
num1|第一个数|<code>number</code>|-|是
num2|第二个数|<code>number</code>|-|是
